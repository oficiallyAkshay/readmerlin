name: readme check
on:
  push:
  pull_request:
  schedule:
    - cron: "0 9 * * 1"
permissions:
  contents: read
jobs:
  readmerlin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oficiallyAkshay/readmerlin@<sha>   # <comment>
