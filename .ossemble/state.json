{
  "optins": {
    "TRAFFIC_TOKEN": "clonometer.yml needs its own fine-grained TRAFFIC_TOKEN secret to read and write clone and view counts, since the built-in GITHUB_TOKEN cannot do that.",
    "package-lock.json": "readmerlin is a real npm project and npm ci needs a committed lockfile for reproducible installs, unlike ossemble's own shipped-bundle repos."
  }
}
